'use client';
import React, { useCallback, useEffect, useState } from 'react';

const KB: React.FC = () => {
  useEffect(() => {
    location.replace('https://meplay.netlify.app/#/keyboard')
  }, [])

  return (
    <></>
  );
};

export default KB;