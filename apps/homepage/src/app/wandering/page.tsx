'use client';
import React, { useCallback, useEffect, useState } from 'react';

const Carnival: React.FC = () => {
  useEffect(() => {
    location.replace('https://meplay.netlify.app/#/wandering?vy=10&mode=arc')
  }, [])

  return (
    <></>
  );
};

export default Carnival;