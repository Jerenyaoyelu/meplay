'use client';
import React, { useCallback, useEffect, useState } from 'react';

const RC: React.FC = () => {
  useEffect(() => {
    location.replace('https://meplay.netlify.app/')
  }, [])

  return (
    <></>
  );
};

export default RC;